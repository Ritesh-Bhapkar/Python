import os

def list_files(path):
    for root, dirs, files in os.walk(path):   # os.walk goes through all subfolders
        for file in files:
            print(file)   # only prints filename, not full path

# Example usage
path = r"C:\Users\YourName\Documents"   # replace with your folder path
list_files(path)
